<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-5">

            <script>
                function data(){
                    return {
                        messageSuccess:"{{ 'customer.custom.success' | t }}",
                        messageError:"{{ 'customer.custom.error' | t }}",
                    }
                }
            </script>

            <div class="alert-card" id="alert-card">
                {{ 'customer.custom.success' | t }}
            </div>

            <!-- <h1>{{ page.title }}</h1> -->
            <div class="card-customer">
                <h2>{{ 'customer.custom.title' | t }}</h2>
                <input placeholder="{{ 'contact.form.email' | t }}" id="customerEmail" class="card-customer__input" type="text">
                <button class="btn card-customer__submit" id="customerSubmit">{{ 'customer.custom.button_submit' | t }}</button>
            </div>
        </div>
    </div>
</div>